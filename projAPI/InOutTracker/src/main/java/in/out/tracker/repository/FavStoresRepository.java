package in.out.tracker.repository;

import in.out.tracker.model.FavStores;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FavStoresRepository extends JpaRepository<FavStores, Long> {
}
